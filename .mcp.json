{
  "mcpServers": {
    "graphite-atlas": {
      "command": "npx",
      "args": ["-y", "-p", "@graphite-atlas/mcp-server", "graphite-mcp"],
      "env": {
        "GRAPHITE_ACCESS_TOKEN": "${GRAPHITE_ACCESS_TOKEN}"
      }
    }
  }
}
